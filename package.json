{
  "name": "smackdown",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
  "build": "vite build",
  "build:render": "VITE_API_BASE_URL=\"https://smackdown-r2qj.onrender.com\" vite build",
    "preview": "vite preview",
    "deploy": "firebase deploy --only hosting",
    "deploy:render": "npm run build:render && firebase deploy --only hosting"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
